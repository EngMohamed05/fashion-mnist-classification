# fashion-mnist-classification
A fully connected neural network implemented in PyTorch to classify Fashion-MNIST images. The notebook includes dataset preprocessing, model architecture definition, training loop, accuracy evaluation, and visualization of model predictions.
